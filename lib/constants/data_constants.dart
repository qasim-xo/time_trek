DateTime currentTime = DateTime.now();

enum Priority { low, medium, high }

enum PomodoroTimerType {
  focusSession,
  shortBreak,
  longBreak,
}

enum PomodoroTimerType {
  focusSession,
  shortBreak,
  longBreak,
}
